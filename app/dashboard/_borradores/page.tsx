// "use client";

export default function DraftPage() {
  //   const articlesDraft = JSON.parse(window.localStorage.getItem("draft"));
  //   console.log(articlesDraft);
  return (
    <span>En construcción</span>
    // <div>
    //   {articlesDraft.map((article, index) => (
    //     <article key={index}>{JSON.stringify(article)}</article>
    //   ))}
    // </div>
  );
}
